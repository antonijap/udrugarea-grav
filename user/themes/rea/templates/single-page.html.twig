{% extends 'base.html.twig' %}

{% block content %}
  <div class="single-page">
    <div class="grid-x">
      <div class="cell">
        {% include 'partials/breadcrumbs.html.twig' %}
        <h1>{{ page.title }}</h1>
        <p>{{ page.content }}</p>
      </div>
    </div>
  </div>
{% endblock %}
